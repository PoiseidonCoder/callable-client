export enum Role {
  ROLE_ADMIN = "ROLE_ADMIN",
  ROLE_USER = "ROLE_USER",
}

export enum FriendStatus {
  PENDING = "PENDING",
  ACCEPTED = "ACCEPTED",
  BLOCKED = "BLOCKED",
  REJECTED = "REJECTED",
}

export enum Presence {
  ONLINE = "ONLINE",
  OFFLINE = "OFFLINE",
}
